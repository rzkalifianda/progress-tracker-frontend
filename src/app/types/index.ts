export type Timeline={
    date: string;
    otherProjects: string;
    projects: string[];
}

export type ResponseObject = {
    success: boolean;
    data: any;
    message: string;
}
export type ResponseObject = {
    success: boolean;
    data: any;
    message: string;
}